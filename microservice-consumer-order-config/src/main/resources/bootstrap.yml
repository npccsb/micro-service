spring:
  application:
    name: microservice-consumer-order-config    # 对应config server所获取的配置文件的{application}
  cloud:
    config:
      #username: user1
      #password: password1
      profile: default
      label: master
      uri: http://192.168.56.1:8080/
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest